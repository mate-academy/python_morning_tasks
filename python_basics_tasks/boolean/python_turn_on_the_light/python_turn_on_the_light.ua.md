У цьому завданні ти навчишся працювати з логічним оператором `not`.  
На екрані ти бачиш змінну `is_light_on`. Використовуючи `print()` та логічний оператор `not` зміни значення змінної `is_light_on` на протилежне.